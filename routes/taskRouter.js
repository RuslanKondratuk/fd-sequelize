const {Router} = require('express');

const taskRouter = Router()

// taskRouter.post('/', UserController.createUser);
// taskRouter.get('/', UserController.getAllUsers);
// taskRouter.get('/:userId', UserController.getOneUser);
// taskRouter.delete('/:userId', UserController.deleteUser);
// taskRouter.put('/:userId', UserController.updateUser)

module.exports = taskRouter;